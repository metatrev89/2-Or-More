{
  "name": "twoplus-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "2+ backend — Supabase (auth/db) + Cloudflare (Workflows/Containers/R2) media pipeline",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev:api": "tsx src/api/node-server.ts",
    "dev:worker": "tsx src/queue/worker.ts",
    "demo": "tsx scripts/dev-demo.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
